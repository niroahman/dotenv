-- nvim-treesitter-textobjects removed: depends on nvim-treesitter plugin which was removed.
-- Textobject motions (if needed later) can be reimplemented with nvim 0.12 builtin treesitter queries.
return {}
